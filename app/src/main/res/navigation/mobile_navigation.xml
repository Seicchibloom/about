<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="com.pack.galabout.MainActivity"
        android:label="login"
        tools:layout="@layout/login" />
    <activity
        android:id="@+id/attractions"
        android:name="com.pack.galabout.Attractions"
        android:label="attractions"
        tools:layout="@layout/attractions" />
    <action
        android:id="@+id/action_global_mainActivity"
        app:destination="@id/mainActivity" />
    <activity
        android:id="@+id/homepageActivity"
        android:name="com.pack.galabout.HomepageActivity"
        android:label="HomepageActivity" />
    <activity
        android:id="@+id/hotelBooking"
        android:name="com.pack.galabout.HotelBooking"
        android:label="acommod_kotlin"
        tools:layout="@layout/acommod_kotlin" />
</navigation>